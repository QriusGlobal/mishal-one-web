---
// import { SEO } from 'astro-seo';
// import { SITE_NAME, SITE_URL, SITE_DESCRIPTION } from '../config';
// import { VERSION, BUILD_TIMESTAMP } from '../version';
// import '../styles/material-tokens.css';

// SEO and metadata commented out until branding is finalised.
// Uncomment the above imports and the <SEO /> block below when ready.

// export interface Props {
//   title?: string;
//   description?: string;
//   image?: string;
// }

// const canonicalURL = new URL(Astro.url.pathname, Astro.site).toString();
// const { title = SITE_NAME, description = SITE_DESCRIPTION, image = '/og-placeholder.png' } = Astro.props;
// const fullTitle = title === SITE_NAME ? title : `${title} — ${SITE_NAME}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>mishal.one</title>

    <!-- SEO metadata — uncomment when branding is ready
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta name="version" content={VERSION} />
    <meta name="build-timestamp" content={BUILD_TIMESTAMP} />
    <link rel="canonical" href={canonicalURL} />
    <SEO
      title={fullTitle}
      description={description}
      openGraph={{
        basic: { title: fullTitle, type: 'website', image: `${SITE_URL}${image}`, url: canonicalURL },
        optional: { description, siteName: SITE_NAME },
      }}
      twitter={{ card: 'summary_large_image', title: fullTitle, description, image: `${SITE_URL}${image}` }}
    />
    -->
  </head>
  <body>
    <slot />
  </body>
</html>
